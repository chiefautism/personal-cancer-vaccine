# Sample Output

Example output files for reference. These are **illustrative placeholders** showing the expected format — actual values will differ when you run the pipeline.

- `top10_neoantigens.tsv` — Ranked neoantigen candidates with binding affinity scores

Run the pipeline to generate real predictions:

```bash
bash easy_mode/run_easy_mode.sh
```
